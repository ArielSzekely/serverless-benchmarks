# Copyright 2020-2025 ETH Zurich and the SeBS authors. All rights reserved.
# Use the CPU-only PyTorch wheel index as the primary index so pip never
# downloads CUDA variants.  --extra-index-url re-adds PyPI so non-torch
# packages (Pillow) are still resolved normally.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple/
torch
torchvision
Pillow
